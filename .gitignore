# 构建产物 —— 不入库
# 背景:本仓库此前把 target/ 也纳入了跟踪(1077 个文件、磁盘 3.3G),导致每次构建都产生
# 大量无关 diff。已从索引移除(工作区文件保留)。
# 注意:历史提交里仍留有旧的 target/ 副本,仓库体积不会因本文件变小;
# 需要彻底瘦身时用 git filter-repo / BFG 重写历史(属破坏性操作,需另行确认)。
#
# 写法说明:gitignore 不支持行尾注释(整行都会被当作模式),注释必须独占一行。
/target/

# 运行时产物:logs/ 下都是本机跑出来的日志、pid、实例代理配置(newproxy.conf),
# 每次验收/演练都会产生新 diff;目录由 scripts/lib.sh:16 自动创建,无需入库。
logs/

# 本机运行时/工具链产物
.rdsctl-mysql/
.gradle-home/
.DS_Store

# DSH 智能体会话产物(每轮报告),属本机工具链输出,不入库
.dsh/
